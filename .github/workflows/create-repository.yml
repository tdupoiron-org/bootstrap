---
name: Create Repository
on:
  issues:
    types:
      - opened
      - reopened

jobs:

  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: Hello world action step
        run: echo "Hello World!"

  display_event: 
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{ github.event.issue.id }}
      - run: echo ${{ github.event.issue.title }}
      - run: |
          echo ${{ github.event.issue.body }} >> body.txt
          cat body.txt
          echo $(awk '/### Repository name/{getline; getline; print}' body.txt)
